export class Data {
    constructor(
        public type: string,
        public name: string,
        public value: number,
        public offset: number,
        public unit: string,
        public timestamp: number
    ) {

    }
}